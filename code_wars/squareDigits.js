// R: take a number and square every digit in it, return the squares concatenated as number
// E: num = 9119 => 811181 || num =765 => 493625
// A: convert number into an array, may have to convert it into a string first, each element in the array get multiplied by itself, concatenate into string, change into number

function squareDigits(num) {
  return 0;
}
